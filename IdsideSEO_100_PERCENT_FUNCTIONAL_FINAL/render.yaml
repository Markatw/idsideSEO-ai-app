services:
  - type: web
    name: idsideseo-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 src.main:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite:///data/idsideseo.db
      - key: OPENAI_API_KEY
        sync: false
    disk:
      name: idsideseo-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1

